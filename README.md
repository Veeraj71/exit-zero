# DevOps Command Quiz Engine

A lightweight Python Flask API that acts as a terminal troubleshooting mini-game.

## How to Run Locally
1. Install dependencies: `pip install -r requirements.txt`
2. Launch the engine: `python app.py`